/**
 * Screen 7.3 — Connect your agent (placeholder).
 * WP-2 replaces this file with the full implementation; WP-0 only registers
 * the route so navigation and shell tests work end to end.
 */
export default function ConnectAgent() {
  return (
    <div className="mx-auto max-w-shell px-6 py-8" data-testid="screen-ConnectAgent">
      <div className="mb-1 text-2xs font-bold uppercase tracking-widest text-faint">
        Screen 7.3 · placeholder (WP-2)
      </div>
      <h1 className="text-lg">Connect your agent</h1>
      <p className="mt-2 max-w-xl text-sm text-muted">Sample-agent picker, fingerprint registration, ownership challenge.</p>
    </div>
  );
}
