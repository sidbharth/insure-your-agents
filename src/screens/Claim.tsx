/**
 * Screen 7.11 — File a claim (placeholder).
 * WP-5 replaces this file with the full implementation; WP-0 only registers
 * the route so navigation and shell tests work end to end.
 */
export default function Claim() {
  return (
    <div className="mx-auto max-w-shell px-6 py-8" data-testid="screen-Claim">
      <div className="mb-1 text-2xs font-bold uppercase tracking-widest text-faint">
        Screen 7.11 · placeholder (WP-5)
      </div>
      <h1 className="text-lg">File a claim</h1>
      <p className="mt-2 max-w-xl text-sm text-muted">Notify, Contain, Evidence, Clocks and decision, Outcome.</p>
    </div>
  );
}
