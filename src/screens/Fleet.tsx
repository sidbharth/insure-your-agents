/**
 * Screen 7.7 — Add additional agents (placeholder).
 * WP-3 replaces this file with the full implementation; WP-0 only registers
 * the route so navigation and shell tests work end to end.
 */
export default function Fleet() {
  return (
    <div className="mx-auto max-w-shell px-6 py-8" data-testid="screen-Fleet">
      <div className="mb-1 text-2xs font-bold uppercase tracking-widest text-faint">
        Screen 7.7 · placeholder (WP-3)
      </div>
      <h1 className="text-lg">Add additional agents</h1>
      <p className="mt-2 max-w-xl text-sm text-muted">Fleet table, CSV import sweep, concentration meter, de-enroll.</p>
    </div>
  );
}
