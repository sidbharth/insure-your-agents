/**
 * Screen 7.2 — Verify your company (placeholder).
 * WP-2 replaces this file with the full implementation; WP-0 only registers
 * the route so navigation and shell tests work end to end.
 */
export default function VerifyCompany() {
  return (
    <div className="mx-auto max-w-shell px-6 py-8" data-testid="screen-VerifyCompany">
      <div className="mb-1 text-2xs font-bold uppercase tracking-widest text-faint">
        Screen 7.2 · placeholder (WP-2)
      </div>
      <h1 className="text-lg">Verify your company</h1>
      <p className="mt-2 max-w-xl text-sm text-muted">Pre-filled KYB form, latency-theater verification, priced skip path.</p>
    </div>
  );
}
