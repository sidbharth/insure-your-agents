/**
 * Screen 7.6 — Your quote (placeholder).
 * WP-3 replaces this file with the full implementation; WP-0 only registers
 * the route so navigation and shell tests work end to end.
 */
export default function Quote() {
  return (
    <div className="mx-auto max-w-shell px-6 py-8" data-testid="screen-Quote">
      <div className="mb-1 text-2xs font-bold uppercase tracking-widest text-faint">
        Screen 7.6 · placeholder (WP-3)
      </div>
      <h1 className="text-lg">Your quote</h1>
      <p className="mt-2 max-w-xl text-sm text-muted">Rate-ladder recap, premium in $ and N, coverage cards, exclusion wall.</p>
    </div>
  );
}
